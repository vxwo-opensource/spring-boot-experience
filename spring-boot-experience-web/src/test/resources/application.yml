experience:
  web:
    authorization:
      api-key:
        enabled: true
        path-rules:
          - /test-api-key;test-key:testor

      bearer:
        enabled: true
        path-rules:
          - /test-bearer/;exclude;optional

