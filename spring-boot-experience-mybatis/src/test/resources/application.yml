spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: password

mybatis:
  configuration:
    map-underscore-to-camel-case: true

sbexp:
  mybatis:
    general-sql:
      reserved-prefix: "`"
      reserved-stuffix: "`"
